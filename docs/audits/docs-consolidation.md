# Documentation Consolidation Pass - 2026-05-18

## Changed

### `README.md`

- Rewrote the repo description to match current config: `project.godot` names
  the running project `Shelf Life`, while `export_presets.cfg` still produces
  `MallcoreSim` artifacts.
- Kept local run, test, deployment, and docs-pointer sections.
- Corrected the Godot version statement: validation CI uses
  `4.6.2-stable`; export CI passes `4.6.2` to `chickensoft-games/setup-godot`.
- Removed links to deleted non-current docs.

### `docs/index.md`

- Rebuilt the index around the docs that remain in the active docs set.
- Removed references to deleted design, contributing, beta checklist,
  interactable matrix, and historical audit reports.
- Documented that dated interaction-audit markdown is generated by
  `tests/audit_run.sh` and is not hand-maintained project documentation.
- Kept the boundary note: `BRAINDUMP.md` is customer voice and not edited by
  documentation passes; `.github/`, `tools/`, `addons/`, and `.aidlc/`
  markdown are not active game docs.

### `docs/content-data.md`

- Corrected the loader description. Current `DataLoader` requires a root
  `"type"` field and looks it up in `_TYPE_ROUTES`; it does not infer content
  type from path or filename.

### `docs/configuration-deployment.md`

- Corrected the Godot version wording for validation vs export workflows.

### `docs/testing.md`

- Corrected the export workflow version wording.

### `docs/style/visual-grammar.md`

- Replaced the previous visual-governance prose with a code-grounded token
  reference for `UIThemeConstants`, `game_theme.tres`, `palette.tres`, and the
  active `retro_games` accent resource.

## Deleted

These files were removed because they were historical, duplicated, planning
or checklist material, or contained future/process claims that were not
verifiable from the current code/config surface:

- `docs/design.md`
- `docs/contributing.md`
- `docs/retro_games_interactable_matrix.md`
- `docs/beta/validation_checklist.md`
- `docs/audits/2026-05-05-audit.md`
- `docs/audits/2026-05-06-audit.md`
- `docs/audits/cleanup-report.md`
- `docs/audits/error-handling-report.md`
- `docs/audits/security-report.md`
- `docs/audits/ssot-report.md`
- `planning/deliverable-audit-cycle16.md`
- `planning/wave-1-implementation-sequence.md`

## Statements Removed As Unverifiable Or Non-Current

- Design-rule language such as "merge-blocker", "out of scope for 1.0", and
  future mechanic constraints. Those are product/process decisions, not facts
  currently enforced by code.
- Manual beta QA requirements around screenshot capture and review attachment.
  The related code exists, but the checklist completion policy is not enforced
  by the repository.
- The old Retro Games interactable matrix, including follow-up placeholders and
  Day 2 leakage notes. A full node-by-node matrix requires a fresh scene audit
  before it can be treated as current documentation.
- Historical error-handling, security, SSOT, cleanup, and interaction-audit
  pass reports. They recorded point-in-time work against older diffs and
  contained stale file names, stale class names, and prior escalation language.
- Planning docs under `planning/`. They described issue sequencing and cycle
  audit state, not current runtime behavior.

## Intentional Gaps

- `BRAINDUMP.md` remains untouched because the pass instructions mark it as
  customer voice.
- Markdown under `.github/` is left in place because those files are GitHub
  issue/PR templates consumed by GitHub in that location.
- Markdown under `addons/` is left in place because it is vendored GUT material.
- Markdown under `tools/aidlc/` is left in place because it documents and
  templates the local AIDLC tool, not the active game.
- Markdown under `.aidlc/` is left in place because it is generated/tooling run
  output outside the active docs boundary described by this pass.

## Validation

- Source of truth inspected: `project.godot`, `export_presets.cfg`,
  `.github/workflows/validate.yml`, `.github/workflows/export.yml`,
  `tests/run_tests.sh`, `scripts/run_godot_tests.sh`,
  `scripts/validate_export_config.sh`, `game/scripts/core/boot.gd`,
  `game/autoload/data_loader.gd`, `game/autoload/content_registry.gd`,
  `game/autoload/game_manager.gd`, `game/scenes/world/game_world.gd`,
  `game/autoload/scene_router.gd`, `game/autoload/modal_queue.gd`,
  `game/autoload/event_bus.gd`, and
  `game/scripts/ui/ui_theme_constants.gd`.
- `python3` Markdown link check over `README.md` and `docs/**/*.md`:
  `Markdown links OK`.
- `bash scripts/validate_export_config.sh`: `Export config validation: OK`.
- `git diff --check -- README.md docs planning`: passed.
- `bash tests/run_tests.sh`: failed in existing GUT coverage after the docs
  edits with `Scripts 364`, `Tests 4414`, `Passing 4408`, `Failing 4`,
  `Risky/Pending 2`. The failing tests are listed under Escalations because
  this pass is docs-only and the failures are in GDScript test contracts.

## Escalations

### Full GUT suite is red outside the docs surface

`bash tests/run_tests.sh` currently fails in code/test contracts that were not
edited by this documentation pass:

- `tests/gut/test_beta_hud.gd::test_activate_seeds_objectives_from_active_controller`
- `tests/gut/test_objective_director.gd::test_note_dismiss_advances_day1_chain_to_talk_to_customer`
- `tests/gut/test_objective_director.gd::test_store_entered_emits_objective_changed`
- `tests/gut/test_objective_director.gd::test_day1_chain_advances_through_each_signal_in_order`

The same run also reports one risky test,
`tests/gut/test_beta_hud.gd::test_sale_toast_skipped_when_item_name_blank`, and
one pending test,
`tests/gut/test_order_panel.gd::test_active_store_changed_reloads_the_catalog`.

Smallest concrete next action: a code/test contract pass on the Day-1
objective copy/completion state and BetaHUD objective seeding behavior, then
rerun `bash tests/run_tests.sh`.
